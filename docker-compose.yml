version: "3.9"

services:
  db:
    image: postgres:17
    container_name: passkey_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: passkey_db
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  passkeys-service:
    build: .
    container_name: passkeys-service
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: passkey_db
      DB_PORT: 5432
      PORT: 8085
      ENV: development
      RP_ID: localhost
      RP_ORIGINS: http://localhost:6334,http://localhost:8085,http://localhost:5173,http://localhost:8081
    ports:
      - "8085:8085"
    restart: always

  manual_test:
    build:
      context: ./manual-test
    container_name: manual_test_server
    ports:
      - "6334:6334"
    restart: always

volumes:
  db_data:

